DB_CONNECTION_STRING=Server=localhost;Database=copilot_router;User Id=sa;Password=your_password_here;
PORT=4242
# Optional: Environment mode (Development or Production)
# In Production mode, the following endpoints are disabled for security:
# - /openapi.json (API documentation)
# - /auth/* (authentication endpoints)
# - /login (web UI)
# - /token (token information)
# ENVIRONMENT=Production
ENVIRONMENT=Development
# Optional: API Key for authenticating requests (like OpenAI API key)
# If set, all API requests must include this key in the Authorization header
# Format: Authorization: Bearer <API_KEY>
API_KEY=